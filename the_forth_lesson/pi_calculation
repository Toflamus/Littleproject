from random import random
from math import sqrt
from time import clock
Dart=10000000
hit=0
# clock()
for i in range(1,Dart+1):
    x,y = random(),random()
    dist=sqrt(x**2+y**2)
    if dist<=1:
        hit+=1
pi=4*hit/Dart
print(pi)
